#ifndef STATE_PHOTO_REVIEW_H
#define STATE_PHOTO_REVIEW_H

#include "../state/app_state.h"

// Create the photo review state instance (statically allocated)
AppState* state_photo_review_create(void);

#endif // STATE_PHOTO_REVIEW_H
