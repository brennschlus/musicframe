#ifndef STATE_PLAYBACK_VIEW_H
#define STATE_PLAYBACK_VIEW_H

#include "../state/app_state.h"

// Create the playback view state instance (statically allocated)
AppState* state_playback_view_create(void);

#endif // STATE_PLAYBACK_VIEW_H
