#ifndef STATE_MAIN_MENU_H
#define STATE_MAIN_MENU_H

#include "../state/app_state.h"

// Create the main menu state instance (statically allocated)
AppState* state_main_menu_create(void);

#endif // STATE_MAIN_MENU_H
